#include <iostream>

int main() {
  int n;
std::cin >> n;
  float x;
  char c;
  std::cin >> x >> c;
  std::cout<<n<<" "<<x<<" "<< c;
  return 0;
}
//basic i/o in cpp 

A PROGRAM TO SWAP TWO VARIABLES IN CPP

#include <iostream>
using namespace std;

// Function to swap two numbers using references
void swap(int &a, int &b) {
    int temp = a; // Store the value of a in temp
    a = b;        // Assign the value of b to a
    b = temp;     // Assign the value of temp (original a) to b
}

int main() {
    int a = 5, b = 10;
    cout << "Before swapping: a = " << a << ", b = " << b << endl;
    swap(a, b); // Call the swap function
    cout << "After swapping: a =
NOTE: we should pass the address as the swapping is local to the function so we passed address over there
